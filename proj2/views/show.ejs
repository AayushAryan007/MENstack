<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> · Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen bg-zinc-900 text-white">
    <div class="max-w-3xl mx-auto px-4 pt-8">
      <a href="/" class="text-sm text-zinc-400 hover:text-zinc-200"
        >&larr; Back</a
      >

      <div class="mt-4 bg-zinc-800/60 rounded-xl shadow-lg p-6">
        <div class="flex items-start justify-between">
          <h1 class="text-2xl font-semibold"><%= title %></h1>
          <span class="text-xs px-2 py-1 rounded bg-blue-600/30 text-blue-300"
            ><%= status %></span
          >
        </div>
        <div class="mt-4 whitespace-pre-wrap text-zinc-200"><%= content %></div>
      </div>
    </div>
  </body>
</html>
